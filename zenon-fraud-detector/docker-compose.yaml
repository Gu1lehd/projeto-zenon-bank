services:
  db.bankf:
    image: mysql:8.4
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
    ports:
      - '3306:3306'
    volumes:
      - mysql.bankf:/var/lib/mysql
volumes:
  mysql.bankf:
